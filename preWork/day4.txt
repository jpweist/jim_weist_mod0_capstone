<h3>Read Chapter 5 on Images HTML from HTML and CSS: Design and Build Websites
Answer these questions about the reading in your mod 0 capstone gist:
  </h3>

<ol>
  <li>In an image element, why is the alt attribute important?
    <p>Answer: The alt attribute provides a text description of the image while describes the image if you cannot see it.</p>
    </li>
<li>What determines if an image element is inline or block?
  <p>Answer: If the image is followed by a block level element like a paragraph then the block level element will sit on a new line. If the img is inside a block level element then any text or other elements will flow around it.</p>
  </li>
<li>What are the benefits of jpg or png image file formats?
  <p>Answer: jpg are good with images with many colors or photographs with suttle differences in color. png are good for fewer colors and transparency. </p>
  </li>
</ol>
<h3>Read Chapter 16 on Images CSS from HTML and CSS: Design and Build Websites
Answer these questions about the reading in your mod 0 capstone gist:
</h3>
<ol>
  <li>What is the benefit of specifying the height and width of images in CSS compared to specifying in the HTML?
    <p>Answer: specifiying the image sizes helps pages load more smoothly because the HTML ancd CSS cod will load before the images and telling the brower how much space to leave for and image allowes it to render the rest of the page without waiting for the image to download.</p>
    </li>
<li>What is an image sprite, and why is it useful?
  <p>Answer: An image sprite is an single image used for several differnt parts of an interface. the advantage of using sprites is that the web browser only needs to request  one image rather than many.</p>
  </li>
</ol>
<p>Continue working with the page you created in CodePen, and add a couple relevant images to your paragraph text.
</p>